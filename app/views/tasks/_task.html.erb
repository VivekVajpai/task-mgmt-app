<%= turbo_frame_tag dom_id(article, "id") do %>
  <div id="task_<%= task.id %>" class="p-4 border-b">
    <h3 class="text-lg font-semibold"><%= task.title %></h3>
    <p><%= task.description %></p>
    <p class="text-sm text-gray-500">Status: <%= task.status %></p>
  </div>
<% end %>
