<h2 class="text-xl font-bold">Tasks for <%= @project.name %></h2>

<%= link_to "New Task", new_project_task_path(@project), class: "bg-green-500 text-white px-4 py-2 rounded" %>

<div class="mt-4">
  <%= render @tasks %>
</div>
