<h1 class="text-2xl font-bold">Projects</h1>

<%= link_to "New Project", new_project_path, data: { turbo_frame: "project_form" }, class: "bg-blue-500 text-white px-4 py-2 rounded" %>

<turbo-frame id="project_form"></turbo-frame>

<div id="projects">
  <%= render @projects %>
</div>
